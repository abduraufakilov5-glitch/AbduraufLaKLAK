"use client";
import { useState } from "react";
import { useRouter } from "next/navigation";
const statuses=["pending","confirmed","processing","shipped","delivered","cancelled","returned"] as const;
const labels:Record<string,string>={pending:"Новый",confirmed:"Подтверждён",processing:"В работе",shipped:"Отправлен",delivered:"Доставлен",cancelled:"Отменён",returned:"Возврат"};
export function OrderStatus({id,initial}:{id:string;initial:string}){const router=useRouter();const [value,setValue]=useState(initial);const [busy,setBusy]=useState(false);const [error,setError]=useState("");async function change(v:string){const previous=value;setValue(v);setBusy(true);setError("");try{const r=await fetch(`/api/orders/${id}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify({status:v})});if(!r.ok){const d=await r.json();throw new Error(d.error??"Не удалось обновить")}router.refresh()}catch(e){setValue(previous);setError(e instanceof Error?e.message:"Не удалось обновить")}finally{setBusy(false)}}return <div className="flex flex-col items-end gap-1"><select aria-label="Статус заказа" disabled={busy} value={value} onChange={e=>void change(e.target.value)} className="rounded-full border border-[var(--line)] bg-[var(--card)] px-3 py-1.5 text-xs font-medium outline-none focus:border-[var(--gold)]">{statuses.map(s=><option key={s} value={s}>{labels[s]}</option>)}</select>{error&&<span className="max-w-40 text-[10px] text-[var(--danger)]">{error}</span>}</div>}
