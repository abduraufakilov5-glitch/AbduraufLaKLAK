"use client";
import { useState } from "react";

interface ProductCard {seo_title_ru:string;seo_title_tg:string;description_ru:string;description_tg:string;short_description:string;characteristics:{name:string;value:string}[];laklak_tags:string[];keywords:string[];hashtags:string[]}
export default function AIStudioPage(){const [file,setFile]=useState<File|null>(null);const [category,setCategory]=useState("hijabs");const [material,setMaterial]=useState("");const [color,setColor]=useState("");const [size,setSize]=useState("");const [price,setPrice]=useState("");const [result,setResult]=useState<ProductCard|null>(null);const [error,setError]=useState("");const [loading,setLoading]=useState(false);
 async function generate(){if(!file)return;if(file.size>5*1024*1024){setError("Фото должно быть меньше 5 МБ");return}setLoading(true);setError("");setResult(null);try{const form=new FormData();form.append("file",file);const upload=await fetch("/api/uploads/product-image",{method:"POST",body:form});const uploaded=await upload.json();if(!upload.ok)throw new Error(uploaded.error??"Не удалось загрузить фото");const imageBase64=await new Promise<string>((resolve,reject)=>{const r=new FileReader();r.onload=()=>resolve(String(r.result).split(",")[1]??"");r.onerror=()=>reject(new Error("Не удалось прочитать фото"));r.readAsDataURL(file)});const res=await fetch("/api/ai/product-card",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({imageBase64,sourceImagePath:uploaded.path,mimeType:file.type,category,material:material||undefined,color:color||undefined,size:size||undefined,price:price?Number(price):undefined})});const data=await res.json();if(!res.ok)throw new Error(data.error??"Не удалось создать карточку");setResult(data)}catch(e){setError(e instanceof Error?e.message:"Не удалось создать карточку")}finally{setLoading(false)}}
 const field=(label:string,value:string,setter:(v:string)=>void,placeholder:string)=><label className="block text-xs font-medium text-[var(--muted)]">{label}<input value={value} onChange={e=>setter(e.target.value)} placeholder={placeholder} className="mt-1.5 w-full rounded-xl border border-[var(--line)] bg-transparent px-3 py-2.5 text-sm outline-none transition focus:border-[var(--gold)]"/></label>;
 return <section className="animate-fade-in space-y-6"><header><p className="mb-2 text-sm font-medium text-[var(--gold)]">AI Studio</p><h1 className="text-3xl font-semibold tracking-tight sm:text-4xl">Карточка товара с AI</h1><p className="mt-2 max-w-2xl text-sm text-[var(--muted)]">Загрузи фото и дай несколько фактов. Gemini подготовит SEO-текст, характеристики и теги без выдуманных свойств.</p></header><div className="grid gap-4 xl:grid-cols-[0.8fr_1.2fr]"><div className="rounded-2xl border border-[var(--line)] bg-[var(--card)] p-5 sm:p-6"><div className="rounded-2xl border border-dashed border-[var(--line)] bg-[color:var(--background)] p-6 text-center"><div className="text-4xl">📷</div><p className="mt-3 text-sm font-medium">Фото товара</p><p className="mt-1 text-xs text-[var(--muted)]">JPG, PNG или WebP · до 5 МБ</p><input className="mt-4 block w-full text-xs" type="file" accept="image/jpeg,image/png,image/webp" onChange={e=>setFile(e.target.files?.[0]??null)}/>{file&&<p className="mt-3 truncate text-xs text-[var(--muted)]">{file.name}</p>}</div><div className="mt-5 space-y-3">{field("Категория",category,setCategory,"hijabs")}{field("Материал",material,setMaterial,"шёлк")}{field("Цвет",color,setColor,"чёрный")}{field("Размер",size,setSize,"90×90 см")}{field("Цена",price,setPrice,"250")}</div>{error&&<p role="alert" className="mt-4 rounded-xl bg-[color:var(--danger)]/10 px-3 py-2.5 text-xs text-[var(--danger)]">{error}</p>}<button disabled={!file||loading} onClick={generate} className="mt-4 w-full rounded-xl bg-[var(--foreground)] px-4 py-3 text-sm font-medium text-[var(--background)] transition hover:opacity-90 disabled:opacity-40">{loading?"Создаём карточку…":"✨ Создать карточку"}</button></div><div className="rounded-2xl border border-[var(--line)] bg-[var(--card)] p-5 sm:p-6">{!result?<div className="flex min-h-[420px] items-center justify-center rounded-2xl bg-[color:var(--background)] p-8 text-center"><div className="max-w-sm"><div className="text-4xl">✨</div><h2 className="mt-4 font-semibold">Здесь появится результат</h2><p className="mt-2 text-sm text-[var(--muted)]">После генерации ты получишь готовую структуру карточки для проверки.</p></div></div>:<div className="space-y-5"><div><p className="text-xs text-[var(--muted)]">SEO-заголовок RU</p><h2 className="mt-1 text-lg font-semibold">{result.seo_title_ru}</h2></div><div className="grid gap-4 sm:grid-cols-2"><div><p className="text-xs text-[var(--muted)]">SEO-заголовок TG</p><p className="mt-1 text-sm">{result.seo_title_tg}</p></div><div><p className="text-xs text-[var(--muted)]">Коротко</p><p className="mt-1 text-sm">{result.short_description}</p></div></div><div><p className="text-xs text-[var(--muted)]">Описание RU</p><p className="mt-1 whitespace-pre-line text-sm leading-6">{result.description_ru}</p></div><div><p className="text-xs text-[var(--muted)]">Описание TG</p><p className="mt-1 whitespace-pre-line text-sm leading-6">{result.description_tg}</p></div><div><p className="text-xs text-[var(--muted)]">Характеристики</p><div className="mt-2 flex flex-wrap gap-2">{result.characteristics.map((c,i)=><span key={`${c.name}-${i}`} className="rounded-full border border-[var(--line)] px-3 py-1.5 text-xs">{c.name}: {c.value}</span>)}</div></div><div><p className="text-xs text-[var(--muted)]">Lak Lak теги</p><p className="mt-2 text-sm text-[var(--muted)]">{result.laklak_tags.join(" · ")}</p></div><div className="flex flex-col-reverse gap-2 sm:flex-row sm:justify-end"><button type="button" onClick={()=>setResult(null)} className="rounded-xl border border-[var(--line)] px-4 py-2.5 text-sm">Создать заново</button><button type="button" className="rounded-xl bg-[var(--foreground)] px-4 py-2.5 text-sm font-medium text-[var(--background)]">Сохранить карточку</button></div></div>}</div></div></section>;
}
