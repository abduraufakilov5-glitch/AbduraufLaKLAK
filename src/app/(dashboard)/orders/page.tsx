import { createClient } from "@/lib/supabase/server";
import { OrderStatus } from "@/components/order-status";
interface OrderRow{id:string;status:string;total:number;created_at:string;customers:{full_name:string|null;phone:string|null}|null}
const statusLabel:Record<string,string>={pending:"Новый",confirmed:"Подтверждён",processing:"В работе",shipped:"Отправлен",delivered:"Доставлен",cancelled:"Отменён",returned:"Возврат"};
export default async function OrdersPage(){const supabase=await createClient();const {data,error}=await supabase.from("orders").select("id,status,total,created_at,customers(full_name,phone)").order("created_at",{ascending:false}).limit(100);const orders=(data??[]) as unknown as OrderRow[];return <section className="animate-fade-in space-y-6"><header className="flex flex-col gap-4 sm:flex-row sm:items-end sm:justify-between"><div><p className="mb-2 text-sm font-medium text-[var(--gold)]">Продажи</p><h1 className="text-3xl font-semibold tracking-tight sm:text-4xl">Заказы</h1><p className="mt-2 text-sm text-[var(--muted)]">От нового заказа до доставки — с безопасной сменой статуса.</p></div><div className="rounded-full border border-[var(--line)] bg-[var(--card)] px-3 py-1.5 text-xs text-[var(--muted)]">{orders.length} последних</div></header>{error?<div className="rounded-2xl border border-[var(--line)] bg-[var(--card)] p-6 text-sm text-[var(--danger)]">Не удалось загрузить заказы.</div>:orders.length===0?<div className="rounded-2xl border border-dashed border-[var(--line)] bg-[var(--card)] p-10 text-center"><div className="text-4xl">🛍️</div><h2 className="mt-4 font-semibold">Заказов пока нет</h2><p className="mt-2 text-sm text-[var(--muted)]">Когда появится первый заказ, он автоматически будет показан здесь.</p></div>:<div className="grid gap-3">{orders.map(order=><article key={order.id} className="rounded-2xl border border-[var(--line)] bg-[var(--card)] p-5"><div className="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between"><div><div className="flex items-center gap-2"><span className="font-mono text-xs text-[var(--muted)]">#{order.id.slice(0,8)}</span><span className="rounded-full bg-[color:var(--background)] px-2.5 py-1 text-[10px] font-medium">{statusLabel[order.status]??order.status}</span></div><p className="mt-2 text-sm font-medium">{order.customers?.full_name||order.customers?.phone||"Покупатель без имени"}</p><p className="mt-1 text-xs text-[var(--muted)]">{new Date(order.created_at).toLocaleString("ru-RU")}</p></div><div className="flex items-center gap-4"><div className="text-right"><p className="text-[11px] text-[var(--muted)]">Сумма</p><p className="text-lg font-semibold">{Number(order.total).toLocaleString("ru-RU",{minimumFractionDigits:2,maximumFractionDigits:2})} TJS</p></div><OrderStatus id={order.id} initial={order.status}/></div></div></article>)}</div>}</section>}
