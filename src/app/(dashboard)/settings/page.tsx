import { createClient } from "@/lib/supabase/server";

export default async function SettingsPage(){
 const supabase=await createClient();
 const {data:{user}}=await supabase.auth.getUser();
 const {data:profile}=user?await supabase.from("profiles").select("full_name,role").eq("id",user.id).single():{data:null};
 return <section className="animate-fade-in space-y-6"><header><p className="mb-2 text-sm font-medium text-[var(--gold)]">Рабочее пространство</p><h1 className="text-3xl font-semibold tracking-tight sm:text-4xl">Настройки</h1><p className="mt-2 text-sm text-[var(--muted)]">Профиль и основные параметры Dilyas Shop.</p></header><div className="grid gap-4 lg:grid-cols-2"><div className="rounded-2xl border border-[var(--line)] bg-[var(--card)] p-6"><p className="text-sm font-medium">Профиль</p><div className="mt-5 space-y-4"><div><p className="text-xs text-[var(--muted)]">Email</p><p className="mt-1 text-sm">{user?.email??"—"}</p></div><div><p className="text-xs text-[var(--muted)]">Имя</p><p className="mt-1 text-sm">{profile?.full_name||"Не указано"}</p></div><div><p className="text-xs text-[var(--muted)]">Роль</p><p className="mt-1 inline-flex rounded-full bg-[color:var(--background)] px-3 py-1.5 text-xs font-medium">{profile?.role??"—"}</p></div></div></div><div className="rounded-2xl border border-[var(--line)] bg-[var(--card)] p-6"><p className="text-sm font-medium">Интеграции</p><div className="mt-5 space-y-3"><div className="flex items-center justify-between rounded-xl border border-[var(--line)] p-4"><div><p className="text-sm font-medium">Gemini AI</p><p className="mt-1 text-xs text-[var(--muted)]">Генерация текста и визуальных карточек.</p></div><span className="rounded-full bg-[color:var(--background)] px-2.5 py-1 text-[10px] font-medium">Подключается через ENV</span></div><div className="flex items-center justify-between rounded-xl border border-[var(--line)] p-4"><div><p className="text-sm font-medium">Lak Lak</p><p className="mt-1 text-xs text-[var(--muted)]">Маркетплейс для размещения товаров.</p></div><span className="text-xs text-[var(--muted)]">Подготовлено</span></div><div className="flex items-center justify-between rounded-xl border border-[var(--line)] p-4"><div><p className="text-sm font-medium">Telegram</p><p className="mt-1 text-xs text-[var(--muted)]">Архитектура уведомлений готова к подключению.</p></div><span className="text-xs text-[var(--muted)]">Опционально</span></div></div></div></div></section>;
}
